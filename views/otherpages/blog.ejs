<!DOCTYPE html>
<html>
<head>
  <title>Blog Page</title>

</head>
<body>
  <div class="container">
    <% blogs.forEach(blog => { %>
      <h1><%= blog.title %></h1>
      <img src="<%= blog.image %>" alt="Blog Image">
      <p><%= blog.data %></p>
      <p>Published on: <%= blog.date.toDateString() %></p>
    
    <% }); %>
  </div>
</body>
</html>
