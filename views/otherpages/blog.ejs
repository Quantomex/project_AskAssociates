<!DOCTYPE html>
<html>
<head>
  <title>Blog Page</title>
  <!-- Add your CSS and other head elements here -->
</head>
<body>
  <div class="container">
    <% blogs.forEach(blog => { %>
      <h1><%= blog.title %></h1>
      <img src="<%= blog.image %>" alt="Blog Image">
      <p><%= blog.data %></p>
      <p>Published on: <%= blog.date.toDateString() %></p>
      <!-- Add edit and delete links as needed -->
    <% }); %>
  </div>
</body>
</html>
